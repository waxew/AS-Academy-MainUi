# برنامه جامع آموزش CSS

## مبانی
CSS و نقش آن، پیش‌نیاز HTML، اتصال CSS، Syntax، Selector/Property/Value، Comment، Cascade/Inheritance/Specificity، رنگ‌ها، واحدها، اندازه‌ها، Box Model، box-sizing، متن و فونت، background/border/shadow، display.

## مقدماتی
Selectorهای کامل و combinatorها، attribute selector، pseudo-class/pseudo-element، position و stacking context، overflow/object-fit، Flexbox، Grid، Responsive/Mobile First، Media Queries، فرم/جدول/لیست، Custom Properties، calc/min/max/clamp، transform/transition/animation.

## پیشرفته
Logical Properties و RTL، Container Queries، Subgrid، CSS Nesting، :is/:where/:has/:not، aspect-ratio، Grid پیشرفته، multi-column، mask/clip-path/filter/backdrop-filter، gradients، scroll snap، View Transitions و scroll-driven animation با progressive enhancement، typography و variable fonts، BEM/OOCSS/SMACSS/utility-first، Design Tokens/System، CSS Modules/CSS-in-JS، Sass، Bootstrap، Tailwind، DevTools و Performance.

## تخصصی
Rendering Pipeline، Cascade Layers، theme/Dark Mode، reusable components، Accessibility (focus/contrast/reduced-motion/forced-colors)، print CSS، PWA styling، RTL فارسی، cross-browser و @supports، Progressive Enhancement، visual regression، Core Web Vitals، refactoring در مقیاس تیمی و Component Library.

## پروژه‌ها
کارت پروفایل، Landing Page، داشبورد RTL، فروشگاه responsive، پنل مدیریت، Design System، سایت چندصفحه‌ای با Dark Mode و پروژه نهایی production-ready.

هر فصل: توضیح فارسی مفصل + مثال اجرایی + نکات + خطاهای رایج + تمرین + Quiz + آزمون + پروژه.
